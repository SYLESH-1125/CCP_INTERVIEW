version: '3.9'

services:
  interview-ai:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    env_file:
      - ./backend/.env
    restart: always

# Use 'docker-compose up --build' to run the entire project